/*
    Цикл
*/

/*

    У Стива было 10 кусочков пиццы
    Каждый день/час Стив съедает по пицце
    Если конфеты кончились - Стив не сможет кушать пиццу
 */
let pizza = 10;
 while (pizza) { // пока условие в скобочках возвращает true, 
console.log ("Стив бесконечно ест одну пиццу");
pizza--; // pizza = pizza - 1 (декремент)
}
console.log ("Стив съел всю пиццу, даже не поделился");
/*
Данное число от 2 - 100 (вкл)
При помощи цикла необходимо вывести все чёт. числа до заданного числа (вкл)
*/
//let n = Math.floor(Math.random() * 101);
let n = Math.random();
//0 <= n < 1
n = n * 3; // 0 <= n < 3
n = n + 2; // 2 <= n < 5
n = Math.floor(n);
let step = 2;

console.log(`данное число: ${n}`)
let m;
let min = 5, max = 20;
//m = math.random() * (max -min) + min;
//m = MAth.floor(m);
while (step <= n) {
    // 3 / 2 = 1
    if (step % 2 === 0){
    console.log(step);
    }
  step++;
}

// Данно чилсло т (n - натуральное число) от 1-5 (вкл)
//вывести все варианты написания чисел от 1 до n 